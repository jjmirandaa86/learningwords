const FirstLetterUpperCase = (str) =>
	str.charAt(0).toUpperCase() + str.slice(1).toLowerCase();

const UpperCase = (str) => str.toUpperCase();

const LowerCase = (str) => str.LowerCase();

export { FirstLetterUpperCase, UpperCase, LowerCase };
